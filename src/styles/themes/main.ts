import { borderWidths, radii } from '../tokens/borders';
import { colors } from '../tokens/colors';
import { fonts, fontSizes, fontWeights, lineHeights } from '../tokens/fonts';
import { shadows } from '../tokens/shadows';
import { space } from '../tokens/space';

const main = {
  colors,
  fonts,
  fontWeights,
  fontSizes,
  lineHeights,
  space,
  radii,
  borderWidths,
  shadows,
};

export default main;
